import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.thumnail.rubin',
  appName: 'thumbale maker',
  webDir: 'dist/public'
};

export default config;
